<h1>Article Listing</h1>
<p>
  <%= link_to 'Home Page', controller: 'welcome' %> | 
  <%= link_to 'Create new article', new_article_path %>
</p>
<table>
  <tr>
    <th>Title</th>
    <th>Number of Unique Words</th>
    <th colspan="3"></th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td style="text-align: center;"><%= article.text.downcase.split.uniq.size %></td>
      <td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to "Delete", article_path(article), data: { turbo_method: "delete", turbo_confirm: "Are you sure?" } %></td>
    </tr>
  <% end %>
</table>